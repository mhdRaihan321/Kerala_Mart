<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .headerofuser{
            display: none;
        }
        .footer{
            display: none;
        }
        .reset-form {
    background-color: #fff;
    padding: 20px;
    border-radius: 5px;
    box-shadow: -5px 10px 20px 20px rgb(194 181 181);
    width: 500px;
    height: 500px;
        }
        .reset-form label {
            font-weight: bold;
               margin-bottom: -18px;
            display: block;
        }
        .reset-form input[type=password] {
            width: calc(100% - 10px);
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 3px;
        }
        .reset-form button {
            background-color: #4CAF50;
            color: white;
    padding: 5px 180px;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }
        .reset-form button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="reset-form">
        <h2 style="text-align: center;    margin-top: 100px;">Password Reset</h2>
        <form action="/reset/{{token}}" style="margin-top: 40px;" method="POST">
            <label for="password">New Password:</label><br>
            <input type="password" id="password" name="password" required><br><br>
            <button  class="btn btn-success" type="submit">Reset Password</button>
        </form>
    </div>
</body>
</html>
